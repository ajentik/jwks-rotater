{{- if .Values.prometheus.enable }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "jwks-rotater.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    control-plane: controller-manager
  name: {{ include "jwks-rotater.resourceName" (dict "suffix" "controller-manager-metrics-monitor" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:
    - path: /metrics
      port: https
      scheme: https
      bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      tlsConfig:
        {{- if .Values.prometheus.tlsSecretName }}
        serverName: {{ include "jwks-rotater.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ .Release.Namespace }}.svc
        insecureSkipVerify: false
        ca:
          secret:
            name: {{ .Values.prometheus.tlsSecretName }}
            key: ca.crt
        cert:
          secret:
            name: {{ .Values.prometheus.tlsSecretName }}
            key: tls.crt
        keySecret:
          name: {{ .Values.prometheus.tlsSecretName }}
          key: tls.key
        {{- else }}
        insecureSkipVerify: true
        {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "jwks-rotater.name" . }}
      control-plane: controller-manager
{{- end }}
